# Environment variables for docker-compose
# Copy this file to .env and update the values

# ========================================
# Container Names (Optional)
# ========================================
POSTGRES_CONTAINER_NAME=roombooking-postgres
API_CONTAINER_NAME=roombooking-api
FRONTEND_CONTAINER_NAME=roombooking-frontend

# ========================================
# Port Mappings
# ========================================
POSTGRES_PORT=5432
API_PORT=5000
FRONTEND_PORT=3000

# ========================================
# PostgreSQL Configuration
# ========================================
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres123
POSTGRES_DB=roombooking

# ========================================
# Database Connection (for API)
# ========================================
DB_HOST=postgres
DB_PORT=5432
DB_NAME=roombooking
DB_USER=postgres
DB_PASSWORD=postgres123

# ========================================
# JWT Configuration
# ========================================
# IMPORTANT: Change JWT_SECRET in production!
# Generate a secure random string (min 32 characters)
JWT_SECRET=YourSuperSecretKeyForJWTTokenGeneration123456789
JWT_ISSUER=RoomBookingAPI
JWT_AUDIENCE=RoomBookingClient
JWT_EXPIRATION_MINUTES=60

# ========================================
# CORS Configuration
# ========================================
# Comma-separated list of allowed origins
CORS_ORIGINS=http://localhost:3000,http://localhost:80,http://frontend

# ========================================
# API Configuration
# ========================================
ASPNETCORE_ENVIRONMENT=Production
ENABLE_SWAGGER=true

# ========================================
# Frontend Configuration
# ========================================
VITE_API_URL=http://localhost:5000/api

# ========================================
# Production Security Notes
# ========================================
# 1. Change all default passwords
# 2. Generate a strong JWT_SECRET (use: openssl rand -base64 32)
# 3. Set ENABLE_SWAGGER=false in production
# 4. Use HTTPS URLs in production
# 5. Restrict CORS_ORIGINS to your actual domain
# 6. Never commit the .env file to version control
